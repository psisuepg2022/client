export type Address = {
  id: string;
  city: string;
  state: string;
  zipCode: string;
  publicArea: string;
  district: string;
};

export type FormAddress = {
  city: string;
  state: string;
  zipCode: string;
  publicArea: string;
  district: string;
};
