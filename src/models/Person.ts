export type Person = {
  id: string;
  name: string;
  email?: string;
  contactNumber?: string;
  CPF?: string;
  birthDate: string;
};

export type FormPerson = {
  name: string;
  email?: string;
  contactNumber?: string;
  CPF?: string;
  birthDate: string;
};
